var reviewbuddy = {};

/**
 * Inject our code into the page's DOM.
 */
reviewbuddy.injectIntoPage = function() {
	alert("doctor!");
	
}

reviewbuddy.createReview = function() {
	alert("create review");
}